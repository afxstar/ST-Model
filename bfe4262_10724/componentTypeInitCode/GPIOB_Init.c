/*******************************<<config wizard begin>>**************************
<group>Peripheral
    <list>Peripheral name
    <GPIOB=>GPIOB
    <code>$GPIO_PORT$
</group>

<group=pin>Pin Used
    <list>Pin 0
        <DISABLE=>Disable
        <PA0=>Enable
    <code>$pin0Used$
    <list>Pin 1
        <DISABLE=>Disable
        <PA1=>Enable
    <code>$pin1Used$
    <list>Pin 2
        <DISABLE=>Disable
        <PA2=>Enable
    <code>$pin2Used$
    <list>Pin 3
        <DISABLE=>Disable
        <PA3=>Enable
    <code>$pin3Used$
    <list>Pin 4
        <DISABLE=>Disable
        <PA4=>Enable
    <code>$pin4Used$
    <list>Pin 5
        <DISABLE=>Disable
        <PA5=>Enable
    <code>$pin5Used$
    <list>Pin 6
        <DISABLE=>Disable
        <PA6=>Enable
    <code>$pin6Used$
    <list>Pin 7
        <DISABLE=>Disable
        <PA7=>Enable
    <code>$pin7Used$
    <list>Pin 8
        <DISABLE=>Disable
        <PA8=>Enable
    <code>$pin8Used$
    <list>Pin 9
        <DISABLE=>Disable
        <PA9=>Enable
    <code>$pin9Used$
    <list>Pin 10
        <DISABLE=>Disable
        <PA10=>Enable
    <code>$pin10Used$
    <list>Pin 11
        <DISABLE=>Disable
        <PA11=>Enable
    <code>$pin11Used$
    <list>Pin 12
        <DISABLE=>Disable
        <PA12=>Enable
    <code>$pin12Used$
    <list>Pin 13
        <DISABLE=>Disable
        <PA13=>Enable
    <code>$pin13Used$
    <list>Pin 14
        <DISABLE=>Disable
        <PA14=>Enable
    <code>$pin14Used$
    <list>Pin 15
        <DISABLE=>Disable
        <PA15=>Enable
    <code>$pin15Used$
</group>

<group>Pin 0 Set
    <list>Enable Pin Lock
	    <0=>Disable 
		<1=>Enable
	<code>$pLockEn0$
    <list>Mode control
        <GPIO_Mode_IN_FLOATING =>IN_FLOATING	    
        <GPIO_Mode_AIN=>AIN
        <GPIO_Mode_IPD=>IPD
        <GPIO_Mode_IPU=>IPU
        <GPIO_Mode_Out_OD=>Out_OD
        <GPIO_Mode_Out_PP=>Out_PP
        <GPIO_Mode_AF_OD=>AF_OD
        <GPIO_Mode_AF_PP=>AF_PP
    <code>$pin0Mode$
    <list>Speed config
        <GPIO_Speed_10MHz=>10MHZ
        <GPIO_Speed_2MHz=>2MHZ
        <GPIO_Speed_50MHz=>50MHZ
        <code>$pin0Speed$
	<list>Pin Set Or Reset
	    <SET=>Set
		<RESET=>Reset
	<code>$pinData0$
</group>

<group>Pin 1 Set
    <list>Enable Pin Lock
	    <0=>Disable 
		<1=>Enable
	<code>$pLockEn1$
    <list>Mode control
        <GPIO_Mode_IN_FLOATING =>IN_FLOATING	    
        <GPIO_Mode_AIN=>AIN
        <GPIO_Mode_IPD=>IPD
        <GPIO_Mode_IPU=>IPU
        <GPIO_Mode_Out_OD=>Out_OD
        <GPIO_Mode_Out_PP=>Out_PP
        <GPIO_Mode_AF_OD=>AF_OD
        <GPIO_Mode_AF_PP=>AF_PP
    <code>$pin1Mode$
    <list>Speed config
        <GPIO_Speed_10MHz=>10MHZ
        <GPIO_Speed_2MHz=>2MHZ
        <GPIO_Speed_50MHz=>50MHZ
        <code>$pin1Speed$
	<list>Pin Set Or Reset
	    <SET=>Set
		<RESET=>Reset
	<code>$pinData1$
</group>

<group>Pin 2 Set
    <list>Enable Pin Lock
	    <0=>Disable 
		<1=>Enable
	<code>$pLockEn2$
    <list>Mode control
        <GPIO_Mode_IN_FLOATING=>IN_FLOATING	    
        <GPIO_Mode_AIN=>AIN
        <GPIO_Mode_IPD=>IPD
        <GPIO_Mode_IPU=>IPU
        <GPIO_Mode_Out_OD=>Out_OD
        <GPIO_Mode_Out_PP=>Out_PP
        <GPIO_Mode_AF_OD=>AF_OD
        <GPIO_Mode_AF_PP=>AF_PP
    <code>$pin2Mode$
    <list>Speed config
        <GPIO_Speed_10MHz=>10MHZ
        <GPIO_Speed_2MHz=>2MHZ
        <GPIO_Speed_50MHz=>50MHZ
	<code>$pin2Speed$
    <code>$pin2IntTgr$
	<list>Pin Set Or Reset
	    <SET=>Set
		<RESET=>Reset
	<code>$pinData2$
</group>

<group>Pin 3 Set
    <list>Enable Pin Lock
	    <0=>Disable 
		<1=>Enable
	<code>$pLockEn3$
    <list>Mode control
        <GPIO_Mode_IN_FLOATING=>IN_FLOATING	    
        <GPIO_Mode_AIN=>AIN
        <GPIO_Mode_IPD=>IPD
        <GPIO_Mode_IPU=>IPU
        <GPIO_Mode_Out_OD=>Out_OD
        <GPIO_Mode_Out_PP=>Out_PP
        <GPIO_Mode_AF_OD=>AF_OD
        <GPIO_Mode_AF_PP=>AF_PP
	<code>$pin3Mode$
    <list>Speed config
        <GPIO_Speed_10MHz=>10MHZ
        <GPIO_Speed_2MHz=>2MHZ
        <GPIO_Speed_50MHz=>50MHZ
	<code>$pin3Speed$
	<list>Pin Set Or Reset
	    <SET=>Set
		<RESET=>Reset
	<code>$pinData3$
</group>

<group>Pin 4 Set
    <list>Enable Pin Lock
	    <0=>Disable 
		<1=>Enable
	<code>$pLockEn4$
    <list>Mode control
        <GPIO_Mode_IN_FLOATING=>IN_FLOATING	    
        <GPIO_Mode_AIN=>AIN
        <GPIO_Mode_IPD=>IPD
        <GPIO_Mode_IPU=>IPU
        <GPIO_Mode_Out_OD=>Out_OD
        <GPIO_Mode_Out_PP=>Out_PP
        <GPIO_Mode_AF_OD=>AF_OD
        <GPIO_Mode_AF_PP=>AF_PP
    <code>$pin4Mode$
    <list>Speed config
        <GPIO_Speed_10MHz=>10MHZ
        <GPIO_Speed_2MHz=>2MHZ
        <GPIO_Speed_50MHz=>50MHZ
    <code>$pin4Speed$
	<list>Pin Set Or Reset
	    <SET=>Set
		<RESET=>Reset
	<code>$pinData4$
</group>

<group>Pin 5 Set
    <list>Enable Pin Lock
	    <0=>Disable 
		<1=>Enable
	<code>$pLockEn5$
    <list>Mode control
        <GPIO_Mode_IN_FLOATING=>IN_FLOATING	    
        <GPIO_Mode_AIN=>AIN
        <GPIO_Mode_IPD=>IPD
        <GPIO_Mode_IPU=>IPU
        <GPIO_Mode_Out_OD=>Out_OD
        <GPIO_Mode_Out_PP=>Out_PP
        <GPIO_Mode_AF_OD=>AF_OD
        <GPIO_Mode_AF_PP=>AF_PP
    <code>$pin5Mode$
    <list>Speed config
        <GPIO_Speed_10MHz=>10MHZ
        <GPIO_Speed_2MHz=>2MHZ
        <GPIO_Speed_50MHz=>50MHZ
    <code>$pin5Speed$
	<list>Pin Set Or Reset
	    <SET=>Set
		<RESET=>Reset
	<code>$pinData5$
</group>

<group>Pin 6 Set
    <list>Enable Pin Lock
	    <0=>Disable 
		<1=>Enable
	<code>$pLockEn6$
    <list>Mode control
        <GPIO_Mode_IN_FLOATING=>IN_FLOATING	    
        <GPIO_Mode_AIN=>AIN
        <GPIO_Mode_IPD=>IPD
        <GPIO_Mode_IPU=>IPU
        <GPIO_Mode_Out_OD=>Out_OD
        <GPIO_Mode_Out_PP=>Out_PP
        <GPIO_Mode_AF_OD=>AF_OD
        <GPIO_Mode_AF_PP=>AF_PP
    <code>$pin6Mode$
    <list>Speed config
        <GPIO_Speed_10MHz=>10MHZ
        <GPIO_Speed_2MHz=>2MHZ
        <GPIO_Speed_50MHz=>50MHZ
	<code>$pin6Speed$
	<list>Pin Set Or Reset
	    <SET=>Set
		<RESET=>Reset
	<code>$pinData6$
</group>

<group>Pin 7 Set
    <list>Enable Pin Lock
	    <0=>Disable 
		<1=>Enable
	<code>$pLockEn7$
    <list>Mode control
        <GPIO_Mode_IN_FLOATING=>IN_FLOATING	    
        <GPIO_Mode_AIN=>AIN
        <GPIO_Mode_IPD=>IPD
        <GPIO_Mode_IPU=>IPU
        <GPIO_Mode_Out_OD=>Out_OD
        <GPIO_Mode_Out_PP=>Out_PP
        <GPIO_Mode_AF_OD=>AF_OD
        <GPIO_Mode_AF_PP=>AF_PP
    <code>$pin7Mode$
    <list>Speed config
        <GPIO_Speed_10MHz=>10MHZ
        <GPIO_Speed_2MHz=>2MHZ
        <GPIO_Speed_50MHz=>50MHZ
    <code>$pin7Speed$
	<list>Pin Set Or Reset
	    <SET=>Set
		<RESET=>Reset
	<code>$pinData7$
</group>

<group>Pin 8 Set
    <list>Enable Pin Lock
	    <0=>Disable 
		<1=>Enable
	<code>$pLockEn8$
    <list>Mode control
        <GPIO_Mode_IN_FLOATING=>IN_FLOATING	    
        <GPIO_Mode_AIN=>AIN
        <GPIO_Mode_IPD=>IPD
        <GPIO_Mode_IPU=>IPU
        <GPIO_Mode_Out_OD=>Out_OD
        <GPIO_Mode_Out_PP=>Out_PP
        <GPIO_Mode_AF_OD=>AF_OD
        <GPIO_Mode_AF_PP=>AF_PP
    <code>$pin8Mode$
    <list>Speed config
        <GPIO_Speed_10MHz=>10MHZ
        <GPIO_Speed_2MHz=>2MHZ
        <GPIO_Speed_50MHz=>50MHZ
    <code>$pin8Speed$
	<list>Pin Set Or Reset
	    <SET=>Set
		<RESET=>Reset
	<code>$pinData8$
</group>

<group>Pin 9 Set
    <list>Enable Pin Lock
	    <0=>Disable 
		<1=>Enable
	<code>$pLockEn9$
    <list>Mode control
        <GPIO_Mode_IN_FLOATING=>IN_FLOATING	    
        <GPIO_Mode_AIN=>AIN
        <GPIO_Mode_IPD=>IPD
        <GPIO_Mode_IPU=>IPU
        <GPIO_Mode_Out_OD=>Out_OD
        <GPIO_Mode_Out_PP=>Out_PP
        <GPIO_Mode_AF_OD=>AF_OD
        <GPIO_Mode_AF_PP=>AF_PP
    <code>$pin9Mode$
    <list>Speed config
        <GPIO_Speed_10MHz=>10MHZ
        <GPIO_Speed_2MHz=>2MHZ
        <GPIO_Speed_50MHz=>50MHZ
    <code>$pin9Speed$
	<list>Pin Set Or Reset
	    <SET=>Set
		<RESET=>Reset
	<code>$pinData9$
</group>

<group>Pin 10 Set
    <list>Enable Pin Lock
	    <0=>Disable 
		<1=>Enable
	<code>$pLockEn10$
    <list>Mode control
        <GPIO_Mode_IN_FLOATING=>IN_FLOATING	    
        <GPIO_Mode_AIN=>AIN
        <GPIO_Mode_IPD=>IPD
        <GPIO_Mode_IPU=>IPU
        <GPIO_Mode_Out_OD=>Out_OD
        <GPIO_Mode_Out_PP=>Out_PP
        <GPIO_Mode_AF_OD=>AF_OD
        <GPIO_Mode_AF_PP=>AF_PP
    <code>$pin10Mode$
    <list>Speed config
        <GPIO_Speed_10MHz=>10MHZ
        <GPIO_Speed_2MHz=>2MHZ
        <GPIO_Speed_50MHz=>50MHZ
    <code>$pin10Speed$
	<list>Pin Set Or Reset
	    <SET=>Set
		<RESET=>Reset
	<code>$pinData10$
</group>

<group>Pin 11 Set
    <list>Enable Pin Lock
	    <0=>Disable 
		<1=>Enable
	<code>$pLockEn11$
    <list>Mode control
        <GPIO_Mode_IN_FLOATING=>IN_FLOATING	    
        <GPIO_Mode_AIN=>AIN
        <GPIO_Mode_IPD=>IPD
        <GPIO_Mode_IPU=>IPU
        <GPIO_Mode_Out_OD=>Out_OD
        <GPIO_Mode_Out_PP=>Out_PP
        <GPIO_Mode_AF_OD=>AF_OD
        <GPIO_Mode_AF_PP=>AF_PP
    <code>$pin11Mode$
    <list>Speed config
        <GPIO_Speed_10MHz=>10MHZ
        <GPIO_Speed_2MHz=>2MHZ
        <GPIO_Speed_50MHz=>50MHZ
	<code>$pin11Speed$
	<list>Pin Set Or Reset
	    <SET=>Set
		<RESET=>Reset
	<code>$pinData11$
</group>

<group>Pin 12 Set
    <list>Enable Pin Lock
	    <0=>Disable 
		<1=>Enable
	<code>$pLockEn12$
    <list>Mode control
        <GPIO_Mode_IN_FLOATING=>IN_FLOATING	    
        <GPIO_Mode_AIN=>AIN
        <GPIO_Mode_IPD=>IPD
        <GPIO_Mode_IPU=>IPU
        <GPIO_Mode_Out_OD=>Out_OD
        <GPIO_Mode_Out_PP=>Out_PP
        <GPIO_Mode_AF_OD=>AF_OD
        <GPIO_Mode_AF_PP=>AF_PP
    <code>$pin12Mode$
    <list>Speed config
        <GPIO_Speed_10MHz=>10MHZ
        <GPIO_Speed_2MHz=>2MHZ
        <GPIO_Speed_50MHz=>50MHZ
    <code>$pin12Speed$
	<list>Pin Set Or Reset
	    <SET=>Set
		<RESET=>Reset
	<code>$pinData12$
</group>

<group>Pin 13 Set
    <list>Enable Pin Lock
	    <0=>Disable 
		<1=>Enable
	<code>$pLockEn13$
    <list>Mode control
        <GPIO_Mode_IN_FLOATING=>IN_FLOATING	    
        <GPIO_Mode_AIN=>AIN
        <GPIO_Mode_IPD=>IPD
        <GPIO_Mode_IPU=>IPU
        <GPIO_Mode_Out_OD=>Out_OD
        <GPIO_Mode_Out_PP=>Out_PP
        <GPIO_Mode_AF_OD=>AF_OD
        <GPIO_Mode_AF_PP=>AF_PP
    <code>$pin13Mode$
    <list>Speed config
        <GPIO_Speed_10MHz=>10MHZ
        <GPIO_Speed_2MHz=>2MHZ
        <GPIO_Speed_50MHz=>50MHZ
    <code>$pin13Speed$
	<list>Pin Set Or Reset
	    <SET=>Set
		<RESET=>Reset
	<code>$pinData13$
</group>

<group>Pin 14 Set
    <list>Enable Pin Lock
	    <0=>Disable 
		<1=>Enable
	<code>$pLockEn14$
    <list>Mode control
        <GPIO_Mode_IN_FLOATING=>IN_FLOATING	    
        <GPIO_Mode_AIN=>AIN
        <GPIO_Mode_IPD=>IPD
        <GPIO_Mode_IPU=>IPU
        <GPIO_Mode_Out_OD=>Out_OD
        <GPIO_Mode_Out_PP=>Out_PP
        <GPIO_Mode_AF_OD=>AF_OD
        <GPIO_Mode_AF_PP=>AF_PP
    <code>$pin14Mode$
    <list>Speed config
        <GPIO_Speed_10MHz=>10MHZ
        <GPIO_Speed_2MHz=>2MHZ
        <GPIO_Speed_50MHz=>50MHZ
    <code>$pin14Speed$
	<list>Pin Set Or Reset
	    <SET=>Set
		<RESET=>Reset
	<code>$pinData14$
</group>

<group>Pin 15 Set
    <list>Enable Pin Lock
	    <0=>Disable 
		<1=>Enable
	<code>$pLockEn15$
    <list>Mode control
        <GPIO_Mode_IN_FLOATING=>IN_FLOATING	    
        <GPIO_Mode_AIN=>AIN
        <GPIO_Mode_IPD=>IPD
        <GPIO_Mode_IPU=>IPU
        <GPIO_Mode_Out_OD=>Out_OD
        <GPIO_Mode_Out_PP=>Out_PP
        <GPIO_Mode_AF_OD=>AF_OD
        <GPIO_Mode_AF_PP=>AF_PP
    <code>$pin15Mode$
    <list>Speed config
        <GPIO_Speed_10MHz=>10MHZ
        <GPIO_Speed_2MHz=>2MHZ
        <GPIO_Speed_50MHz=>50MHZ
    <code>$pin15Speed$
	<list>Pin Set Or Reset
	    <SET=>Set
		<RESET=>Reset
	<code>$pinData15$
</group>

<dep>
    <type>state
    <trigger>Pin Used->Pin 0 = Disable 
    <action>Pin 0 Set = disable
</dep>

<dep>
    <type>state
    <trigger>Pin Used->Pin 1 = Disable
    <action>Pin 1 Set = Disable
</dep>

<dep>
    <type>state
    <trigger>Pin Used->Pin 2 = Disable
    <action>Pin 2 Set = Disable
</dep>

<dep>
    <type>state
    <trigger>Pin Used->Pin 3 = Disable
    <action>Pin 3 Set = Disable
</dep>
	
<dep>
    <type>state
    <trigger>Pin Used->Pin 4 = Disable
    <action>Pin 4 Set = Disable
</dep>

<dep>
    <type>state
    <trigger>Pin Used->Pin 5 = Disable
    <action>Pin 5 Set = Disable
</dep>

<dep>
    <type>state
    <trigger>Pin Used->Pin 6 = Disable
    <action>Pin 6 Set = disable
</dep>

<dep>
    <type>state
    <trigger>Pin Used->Pin 7 = Disable
    <action>Pin 7 Set = disable
</dep>

<dep>
    <type>state
    <trigger>Pin Used->Pin 8 = Disable
    <action>Pin 8 Set = disable
</dep>

<dep>
    <type>state
    <trigger>Pin Used->Pin 9 = Disable
    <action>Pin 9 Set = disable
</dep>

<dep>
    <type>state
    <trigger>Pin Used->Pin 10 = Disable
    <action>Pin 10 Set = disable
</dep>

<dep>
    <type>state
    <trigger>Pin Used->Pin 11 = Disable
    <action>Pin 11 Set = disable
</dep>

<dep>
    <type>state
    <trigger>Pin Used->Pin 12 = Disable
    <action>Pin 12 Set = disable
</dep>

<dep>
    <type>state
    <trigger>Pin Used->Pin 13 = Disable
    <action>Pin 13 Set = disable
</dep>

<dep>
    <type>state
    <trigger>Pin Used->Pin 14 = Disable
    <action>Pin 14 Set = disable
</dep>

<dep>
    <type>state
    <trigger>Pin Used->Pin 15 = Disable
    <action>Pin 15 Set = disable
</dep>

<dep>
	<type>state
	<trigger>Pin 0 Set->Mode control = IN_FLOATING	
    <action>Pin 0 Set->Pin Set Or Reset = disable
</dep>

<dep>
	<type>state
	<trigger>Pin 1 Set->Mode control = IN_FLOATING	
    <action>Pin 1 Set->Pin Set Or Reset = disable
</dep>

<dep>
	<type>state
	<trigger>Pin 2 Set->Mode control = IN_FLOATING	
    <action>Pin 2 Set->Pin Set Or Reset = disable
</dep>

<dep>
	<type>state
	<trigger>Pin 3 Set->Mode control = IN_FLOATING	
    <action>Pin 3 Set->Pin Set Or Reset = disable
</dep>

<dep>
	<type>state
	<trigger>Pin 4 Set->Mode control = IN_FLOATING	
    <action>Pin 4 Set->Pin Set Or Reset = disable
</dep>

<dep>
	<type>state
	<trigger>Pin 5 Set->Mode control = IN_FLOATING	
    <action>Pin 5 Set->Pin Set Or Reset = disable
</dep>

<dep>
	<type>state
	<trigger>Pin 6 Set->Mode control = IN_FLOATING	
    <action>Pin 6 Set->Pin Set Or Reset = disable
</dep>

<dep>
	<type>state
	<trigger>Pin 7 Set->Mode control = IN_FLOATING	
    <action>Pin 7 Set->Pin Set Or Reset = disable
</dep>

<dep>
	<type>state
	<trigger>Pin 8 Set->Mode control = IN_FLOATING	
    <action>Pin 8 Set->Pin Set Or Reset = disable
</dep>

<dep>
	<type>state
	<trigger>Pin 9 Set->Mode control = IN_FLOATING	
    <action>Pin 9 Set->Pin Set Or Reset = disable
</dep>

<dep>
	<type>state
	<trigger>Pin 10 Set->Mode control = IN_FLOATING	
    <action>Pin 10 Set->Pin Set Or Reset = disable
</dep>

<dep>
	<type>state
	<trigger>Pin 11 Set->Mode control = IN_FLOATING	
    <action>Pin 11 Set->Pin Set Or Reset = disable
</dep>

<dep>
	<type>state
	<trigger>Pin 12 Set->Mode control = IN_FLOATING	
    <action>Pin 12 Set->Pin Set Or Reset = disable
</dep>

<dep>
	<type>state
	<trigger>Pin 13 Set->Mode control = IN_FLOATING	
    <action>Pin 13 Set->Pin Set Or Reset = disable
</dep>

<dep>
	<type>state
	<trigger>Pin 14 Set->Mode control = IN_FLOATING	
    <action>Pin 14 Set->Pin Set Or Reset = disable
</dep>

<dep>
	<type>state
	<trigger>Pin 15 Set->Mode control = IN_FLOATING	
    <action>Pin 15 Set->Pin Set Or Reset = disable
</dep>

<dep>
	<type>state
	<trigger>Pin 0 Set->Mode control = AIN	
    <action>Pin 0 Set->Pin Set Or Reset = disable
</dep>

<dep>
	<type>state
	<trigger>Pin 1 Set->Mode control = AIN	
    <action>Pin 1 Set->Pin Set Or Reset = disable
</dep>

<dep>
	<type>state
	<trigger>Pin 2 Set->Mode control = AIN	
    <action>Pin 2 Set->Pin Set Or Reset = disable
</dep>

<dep>
	<type>state
	<trigger>Pin 3 Set->Mode control = AIN	
    <action>Pin 3 Set->Pin Set Or Reset = disable
</dep>

<dep>
	<type>state
	<trigger>Pin 4 Set->Mode control = AIN	
    <action>Pin 4 Set->Pin Set Or Reset = disable
</dep>

<dep>
	<type>state
	<trigger>Pin 5 Set->Mode control = AIN	
    <action>Pin 5 Set->Pin Set Or Reset = disable
</dep>

<dep>
	<type>state
	<trigger>Pin 6 Set->Mode control = AIN	
    <action>Pin 6 Set->Pin Set Or Reset = disable
</dep>

<dep>
	<type>state
	<trigger>Pin 7 Set->Mode control = AIN	
    <action>Pin 7 Set->Pin Set Or Reset = disable
</dep>

<dep>
	<type>state
	<trigger>Pin 8 Set->Mode control = AIN	
    <action>Pin 8 Set->Pin Set Or Reset = disable
</dep>

<dep>
	<type>state
	<trigger>Pin 9 Set->Mode control = AIN	
    <action>Pin 9 Set->Pin Set Or Reset = disable
</dep>

<dep>
	<type>state
	<trigger>Pin 10 Set->Mode control = AIN	
    <action>Pin 10 Set->Pin Set Or Reset = disable
</dep>

<dep>
	<type>state
	<trigger>Pin 11 Set->Mode control = AIN	
    <action>Pin 11 Set->Pin Set Or Reset = disable
</dep>

<dep>
	<type>state
	<trigger>Pin 12 Set->Mode control = AIN	
    <action>Pin 12 Set->Pin Set Or Reset = disable
</dep>

<dep>
	<type>state
	<trigger>Pin 13 Set->Mode control = AIN	
    <action>Pin 13 Set->Pin Set Or Reset = disable
</dep>

<dep>
	<type>state
	<trigger>Pin 14 Set->Mode control = AIN	
    <action>Pin 14 Set->Pin Set Or Reset = disable
</dep>

<dep>
	<type>state
	<trigger>Pin 15 Set->Mode control = AIN	
    <action>Pin 15 Set->Pin Set Or Reset = disable
</dep>

<dep>
	<type>state
	<trigger>Pin 0 Set->Mode control = IPD	
    <action>Pin 0 Set->Pin Set Or Reset = disable
</dep>

<dep>
	<type>state
	<trigger>Pin 1 Set->Mode control = IPD	
    <action>Pin 1 Set->Pin Set Or Reset = disable
</dep>

<dep>
	<type>state
	<trigger>Pin 2 Set->Mode control = IPD	
    <action>Pin 2 Set->Pin Set Or Reset = disable
</dep>

<dep>
	<type>state
	<trigger>Pin 3 Set->Mode control = IPD	
    <action>Pin 3 Set->Pin Set Or Reset = disable
</dep>

<dep>
	<type>state
	<trigger>Pin 4 Set->Mode control = IPD	
    <action>Pin 4 Set->Pin Set Or Reset = disable
</dep>

<dep>
	<type>state
	<trigger>Pin 5 Set->Mode control = IPD	
    <action>Pin 5 Set->Pin Set Or Reset = disable
</dep>

<dep>
	<type>state
	<trigger>Pin 6 Set->Mode control = IPD	
    <action>Pin 6 Set->Pin Set Or Reset = disable
</dep>

<dep>
	<type>state
	<trigger>Pin 7 Set->Mode control = IPD	
    <action>Pin 7 Set->Pin Set Or Reset = disable
</dep>

<dep>
	<type>state
	<trigger>Pin 8 Set->Mode control = IPD	
    <action>Pin 8 Set->Pin Set Or Reset = disable
</dep>

<dep>
	<type>state
	<trigger>Pin 9 Set->Mode control = IPD	
    <action>Pin 9 Set->Pin Set Or Reset = disable
</dep>

<dep>
	<type>state
	<trigger>Pin 10 Set->Mode control = IPD	
    <action>Pin 10 Set->Pin Set Or Reset = disable
</dep>

<dep>
	<type>state
	<trigger>Pin 11 Set->Mode control = IPD	
    <action>Pin 11 Set->Pin Set Or Reset = disable
</dep>

<dep>
	<type>state
	<trigger>Pin 12 Set->Mode control = IPD	
    <action>Pin 12 Set->Pin Set Or Reset = disable
</dep>

<dep>
	<type>state
	<trigger>Pin 13 Set->Mode control = IPD	
    <action>Pin 13 Set->Pin Set Or Reset = disable
</dep>

<dep>
	<type>state
	<trigger>Pin 14 Set->Mode control = IPD	
    <action>Pin 14 Set->Pin Set Or Reset = disable
</dep>

<dep>
	<type>state
	<trigger>Pin 15 Set->Mode control = IPD	
    <action>Pin 15 Set->Pin Set Or Reset = disable
</dep>

<dep>
	<type>state
	<trigger>Pin 0 Set->Mode control = IPU	
    <action>Pin 0 Set->Pin Set Or Reset = disable
</dep>

<dep>
	<type>state
	<trigger>Pin 1 Set->Mode control = IPU	
    <action>Pin 1 Set->Pin Set Or Reset = disable
</dep>

<dep>
	<type>state
	<trigger>Pin 2 Set->Mode control = IPU	
    <action>Pin 2 Set->Pin Set Or Reset = disable
</dep>

<dep>
	<type>state
	<trigger>Pin 3 Set->Mode control = IPU	
    <action>Pin 3 Set->Pin Set Or Reset = disable
</dep>

<dep>
	<type>state
	<trigger>Pin 4 Set->Mode control = IPU	
    <action>Pin 4 Set->Pin Set Or Reset = disable
</dep>

<dep>
	<type>state
	<trigger>Pin 5 Set->Mode control = IPU	
    <action>Pin 5 Set->Pin Set Or Reset = disable
</dep>

<dep>
	<type>state
	<trigger>Pin 6 Set->Mode control = IPU	
    <action>Pin 6 Set->Pin Set Or Reset = disable
</dep>

<dep>
	<type>state
	<trigger>Pin 7 Set->Mode control = IPU	
    <action>Pin 7 Set->Pin Set Or Reset = disable
</dep>

<dep>
	<type>state
	<trigger>Pin 8 Set->Mode control = IPU	
    <action>Pin 8 Set->Pin Set Or Reset = disable
</dep>

<dep>
	<type>state
	<trigger>Pin 9 Set->Mode control = IPU	
    <action>Pin 9 Set->Pin Set Or Reset = disable
</dep>

<dep>
	<type>state
	<trigger>Pin 10 Set->Mode control = IPU	
    <action>Pin 10 Set->Pin Set Or Reset = disable
</dep>

<dep>
	<type>state
	<trigger>Pin 11 Set->Mode control = IPU	
    <action>Pin 11 Set->Pin Set Or Reset = disable
</dep>

<dep>
	<type>state
	<trigger>Pin 12 Set->Mode control = IPU	
    <action>Pin 12 Set->Pin Set Or Reset = disable
</dep>

<dep>
	<type>state
	<trigger>Pin 13 Set->Mode control = IPU	
    <action>Pin 13 Set->Pin Set Or Reset = disable
</dep>

<dep>
	<type>state
	<trigger>Pin 14 Set->Mode control = IPU	
    <action>Pin 14 Set->Pin Set Or Reset = disable
</dep>

<dep>
	<type>state
	<trigger>Pin 15 Set->Mode control = IPU	
    <action>Pin 15 Set->Pin Set Or Reset = disable
</dep>

*********************************<<config wizard end>>**************************/
#include "stm32f10x.h"
#include "stm32f10x_gpio.h"
#include "stm32f10x_rcc.h"
/*************************************************************
 * GPIOB Initialization
**************************************************************/
void GPIOB_Init()
{
    #if(STRCMP($pin0Used$, DISABLE) == 0 || STRCMP($pin1Used$, DISABLE) == 0 || STRCMP($pin2Used$, DISABLE)== 0 ||\
	STRCMP($pin3Used$, DISABLE) == 0 || STRCMP($pin4Used$, DISABLE) == 0 || STRCMP($pin5Used$, DISABLE)== 0 || \
	STRCMP($pin6Used$, DISABLE) == 0 || STRCMP($pin7Used$, DISABLE) == 0 || STRCMP($pin8Used$, DISABLE)== 0 || \
	STRCMP($pin9Used$, DISABLE) == 0 || STRCMP($pin10Used$, DISABLE) == 0 || STRCMP($pin11Used$, DISABLE)== 0 || \
	STRCMP($pin12Used$, DISABLE) == 0 || STRCMP($pin13Used$, DISABLE) == 0 || STRCMP($pin14Used$, DISABLE)== 0 || \
	STRCMP($pin15Used$, DISABLE) == 0)
    GPIO_InitTypeDef  GPIO_InitStructure;
    #endif
	
    #if($pLockEn0$ || $pLockEn1$ || $pLockEn2$ || $pLockEn3$ || \
    $pLockEn4$ || $pLockEn5$ || $pLockEn6$ || $pLockEn7$ || \
    $pLockEn8$ || $pLockEn9$ || $pLockEn10$ || $pLockEn11$ || \
    $pLockEn12$ || $pLockEn13$ || $pLockEn14$ || $pLockEn15$)
    uint16_t GPIO_LOCK_PIN;
    //
    // The locked pins of GPIOB
    //
    GPIO_LOCK_PIN = $pLockEn0$ | $pLockEn1$ << 1 | $pLockEn2$ << 2 | $pLockEn3$ << 3 | \
    $pLockEn4$ << 4 | $pLockEn5$ << 5 | $pLockEn6$ << 6 | $pLockEn7$ << 7 | \ 
    $pLockEn8$ << 8 | $pLockEn9$ << 9 | $pLockEn10$ << 10 | $pLockEn11$ << 11 |\
    $pLockEn12$ << 12 | $pLockEn13$ << 13 | $pLockEn14$ << 14 | $pLockEn15$ << 15;
	#endif
	
    //
    // Enable GPIOB APB2PeriphClock
    //
    RCC_APB2PeriphClockCmd(RCC_APB2Periph_GPIOB, ENABLE);
	
    #if(STRCMP($pin0Used$, DISABLE) == 0)
    //
    // Set PA0 Pin Mode
    //	
    GPIO_InitStructure.GPIO_Pin = GPIO_Pin_0;
    GPIO_InitStructure.GPIO_Mode = $pin0Mode$;
    GPIO_InitStructure.GPIO_Speed = $pin0Speed$;
    GPIO_Init(GPIOB, &GPIO_InitStructure);
	#if(STRCMP($pin0Mode$, GPIO_Mode_Out_OD) != 0 || STRCMP($pin0Mode$, GPIO_Mode_Out_PP) != 0 || \
	STRCMP($pin0Mode$, GPIO_Mode_AF_OD) != 0 || STRCMP($pin0Mode$, GPIO_Mode_AF_PP) != 0)
    #if(STRCMP($pinData0$, RESET) == 0)
    //
    // Set or Reset PA0 bits of output data
    //
    GPIO_SetBits(GPIOB, GPIO_Pin_0);
    #endif
    #if(STRCMP($pinData0$, SET) == 0)
    //
    // Set or Reset PA0 bits of output data
    //
    GPIO_ResetBits(GPIOB, GPIO_Pin_0);
	#endif
	#endif
    #endif
	
    #if(STRCMP($pin1Used$, DISABLE) == 0)
    //
    // Set PA1 Pin Mode
    //	
    GPIO_InitStructure.GPIO_Pin = GPIO_Pin_1;
    GPIO_InitStructure.GPIO_Mode = $pin1Mode$;
    GPIO_InitStructure.GPIO_Speed = $pin1Speed$;
    GPIO_Init(GPIOB, &GPIO_InitStructure);
	#if(STRCMP($pin1Mode$, GPIO_Mode_Out_OD) != 0 || STRCMP($pin1Mode$, GPIO_Mode_Out_PP) != 0 || \
	STRCMP($pin1Mode$, GPIO_Mode_AF_OD) != 0 || STRCMP($pin1Mode$, GPIO_Mode_AF_PP) != 0)
    #if(STRCMP($pinData1$, RESET) == 0)
    //
    // Set or Reset PA1 bits of output data
    //
    GPIO_SetBits(GPIOB, GPIO_Pin_1);
    #endif
    #if(STRCMP($pinData1$, SET) == 0)
    //
    // Set or Reset PA1 bits of output data
    //
    GPIO_ResetBits(GPIOB, GPIO_Pin_1);
	#endif
    #endif
	#endif
	
    #if(STRCMP($pin2Used$, DISABLE) == 0)
    //
    // Set PA2 Pin Mode
    //	
    GPIO_InitStructure.GPIO_Pin = GPIO_Pin_2;
    GPIO_InitStructure.GPIO_Mode = $pin2Mode$;
    GPIO_InitStructure.GPIO_Speed = $pin2Speed$;
    GPIO_Init(GPIOB, &GPIO_InitStructure);
	#if(STRCMP($pin2Mode$, GPIO_Mode_Out_OD) != 0 || STRCMP($pin2Mode$, GPIO_Mode_Out_PP) != 0 || \
	STRCMP($pin2Mode$, GPIO_Mode_AF_OD) != 0 || STRCMP($pin2Mode$, GPIO_Mode_AF_PP) != 0)
    #if(STRCMP($pinData2$, RESET) == 0)
    //
    // Set or Reset PA2 bits of output data
    //
    GPIO_SetBits(GPIOB, GPIO_Pin_2);
    #endif
    #if(STRCMP($pinData2$, SET) == 0)
	//
    // Set or Reset PA2 bits of output data
    //
    GPIO_ResetBits(GPIOB, GPIO_Pin_2);
	#endif
    #endif
	#endif
	
    #if(STRCMP($pin3Used$, DISABLE) == 0)
    //
    // Set PA3 Pin Mode
    //	
    GPIO_InitStructure.GPIO_Pin = GPIO_Pin_3;
    GPIO_InitStructure.GPIO_Mode = $pin3Mode$;
    GPIO_InitStructure.GPIO_Speed = $pin3Speed$;
    GPIO_Init(GPIOB, &GPIO_InitStructure);
	#if(STRCMP($pin3Mode$, GPIO_Mode_Out_OD) != 0 || STRCMP($pin3Mode$, GPIO_Mode_Out_PP) != 0 || \
	STRCMP($pin3Mode$, GPIO_Mode_AF_OD) != 0 || STRCMP($pin3Mode$, GPIO_Mode_AF_PP) != 0)
    #if(STRCMP($pinData3$, RESET) == 0)
    //
    // Set or Reset PA3 bits of output data
    //
    GPIO_SetBits(GPIOB, GPIO_Pin_3);
    #endif
    #if(STRCMP($pinData3$, SET) == 0)
    //
    // Set or Reset PA3 bits of output data
    //
    GPIO_ResetBits(GPIOB, GPIO_Pin_3);
	#endif
    #endif
	#endif
	
    #if(STRCMP($pin4Used$, DISABLE) == 0)
    //
    // Set PA4 Pin Mode
    //	
    GPIO_InitStructure.GPIO_Pin = GPIO_Pin_4;
    GPIO_InitStructure.GPIO_Mode = $pin4Mode$;
    GPIO_InitStructure.GPIO_Speed = $pin4Speed$;
    GPIO_Init(GPIOB, &GPIO_InitStructure);
	#if(STRCMP($pin4Mode$, GPIO_Mode_Out_OD) != 0 || STRCMP($pin4Mode$, GPIO_Mode_Out_PP) != 0 || \
	STRCMP($pin4Mode$, GPIO_Mode_AF_OD) != 0 || STRCMP($pin4Mode$, GPIO_Mode_AF_PP) != 0)
    #if(STRCMP($pinData4$, RESET) == 0)
	//
    // Set or Reset PA4 bits of output data
    //
    GPIO_SetBits(GPIOB, GPIO_Pin_4);
    #endif
    #if(STRCMP($pinData4$, SET) == 0)
	//
    // Set or Reset PA4 bits of output data
    //
    GPIO_ResetBits(GPIOB, GPIO_Pin_4);
	#endif
    #endif
	#endif
	
    #if(STRCMP($pin5Used$, DISABLE) == 0)
    //
    // Set PA5 Pin Mode
    //	
    GPIO_InitStructure.GPIO_Pin = GPIO_Pin_5;
    GPIO_InitStructure.GPIO_Mode = $pin5Mode$;
    GPIO_InitStructure.GPIO_Speed = $pin5Speed$;
    GPIO_Init(GPIOB, &GPIO_InitStructure);
	#if(STRCMP($pin5Mode$, GPIO_Mode_Out_OD) != 0 || STRCMP($pin5Mode$, GPIO_Mode_Out_PP) != 0 || \
	STRCMP($pin5Mode$, GPIO_Mode_AF_OD) != 0 || STRCMP($pin5Mode$, GPIO_Mode_AF_PP) != 0)
    #if(STRCMP($pinData5$, RESET) == 0)
	//
    // Set or Reset PA5 bits of output data
    //
    GPIO_SetBits(GPIOB, GPIO_Pin_5);
    #endif
    #if(STRCMP($pinData5$, SET) == 0)
	//
    // Set or Reset PA5 bits of output data
    //
    GPIO_ResetBits(GPIOB, GPIO_Pin_5);
	#endif
    #endif
	#endif
	
    #if(STRCMP($pin6Used$, DISABLE) == 0)
    //
    // Set PA6 Pin Mode
    //	
    GPIO_InitStructure.GPIO_Pin = GPIO_Pin_6;
    GPIO_InitStructure.GPIO_Mode = $pin6Mode$;
    GPIO_InitStructure.GPIO_Speed = $pin6Speed$;
    GPIO_Init(GPIOB, &GPIO_InitStructure);
	#if(STRCMP($pin6Mode$, GPIO_Mode_Out_OD) != 0 || STRCMP($pin6Mode$, GPIO_Mode_Out_PP) != 0 || \
	STRCMP($pin6Mode$, GPIO_Mode_AF_OD) != 0 || STRCMP($pin6Mode$, GPIO_Mode_AF_PP) != 0)
    #if(STRCMP($pinData6$, RESET) == 0)
	//
    // Set or Reset PA6 bits of output data
    //
    GPIO_SetBits(GPIOB, GPIO_Pin_6);
    #endif
    #if(STRCMP($pinData6$, SET) == 0)
	//
    // Set or Reset PA6 bits of output data
    //
    GPIO_ResetBits(GPIOB, GPIO_Pin_6);
	#endif
    #endif
	#endif
	
    #if(STRCMP($pin7Used$, DISABLE) == 0)
    //
    // Set PA7 Pin Mode
    //	
    GPIO_InitStructure.GPIO_Pin = GPIO_Pin_7;
    GPIO_InitStructure.GPIO_Mode = $pin7Mode$;
    GPIO_InitStructure.GPIO_Speed = $pin7Speed$;
    GPIO_Init(GPIOB, &GPIO_InitStructure);
	#if(STRCMP($pin7Mode$, GPIO_Mode_Out_OD) != 0 || STRCMP($pin7Mode$, GPIO_Mode_Out_PP) != 0 || \
	STRCMP($pin7Mode$, GPIO_Mode_AF_OD) != 0 || STRCMP($pin7Mode$, GPIO_Mode_AF_PP) != 0)
    #if(STRCMP($pinData7$, RESET) == 0)
	//
    // Set or Reset PA7 bits of output data
    //
    GPIO_SetBits(GPIOB, GPIO_Pin_7);
    #endif
    #if(STRCMP($pinData7$, SET) == 0)
	//
    // Set or Reset PA7 bits of output data
    //
    GPIO_ResetBits(GPIOB, GPIO_Pin_7);
	#endif
    #endif
	#endif
	
    #if(STRCMP($pin8Used$, DISABLE) == 0)
    //
    // Set PA8 Pin Mode
    //	
    GPIO_InitStructure.GPIO_Pin = GPIO_Pin_8;
    GPIO_InitStructure.GPIO_Mode = $pin8Mode$;
    GPIO_InitStructure.GPIO_Speed = $pin8Speed$;
    GPIO_Init(GPIOB, &GPIO_InitStructure);
	#if(STRCMP($pin8Mode$, GPIO_Mode_Out_OD) != 0 || STRCMP($pin8Mode$, GPIO_Mode_Out_PP) != 0 || \
	STRCMP($pin8Mode$, GPIO_Mode_AF_OD) != 0 || STRCMP($pin8Mode$, GPIO_Mode_AF_PP) != 0)
    #if(STRCMP($pinData8$, RESET) == 0)
	//
    // Set or Reset PA8 bits of output data
    //
    GPIO_SetBits(GPIOB, GPIO_Pin_8);
    #endif
    #if(STRCMP($pinData8$, SET) == 0)
	//
    // Set or Reset PA8 bits of output data
    //
    GPIO_ResetBits(GPIOB, GPIO_Pin_8);
	#endif
    #endif
	#endif
	
    #if(STRCMP($pin9Used$, DISABLE) == 0)
    //
    // Set PA9 Pin Mode
    //	
    GPIO_InitStructure.GPIO_Pin = GPIO_Pin_9;
    GPIO_InitStructure.GPIO_Mode = $pin9Mode$;
    GPIO_InitStructure.GPIO_Speed = $pin9Speed$;
    GPIO_Init(GPIOB, &GPIO_InitStructure);
	#if(STRCMP($pin9Mode$, GPIO_Mode_Out_OD) != 0 || STRCMP($pin9Mode$, GPIO_Mode_Out_PP) != 0 || \
	STRCMP($pin9Mode$, GPIO_Mode_AF_OD) != 0 || STRCMP($pin9Mode$, GPIO_Mode_AF_PP) != 0)
    #if(STRCMP($pinData9$, RESET) == 0)
	//
    // Set or Reset PA9 bits of output data
    //
    GPIO_SetBits(GPIOB, GPIO_Pin_9);
    #endif
    #if(STRCMP($pinData9$, SET) == 0)
	//
    // Set or Reset PA9 bits of output data
    //
    GPIO_ResetBits(GPIOB, GPIO_Pin_9);
	#endif
    #endif
	#endif
	
    #if(STRCMP($pin10Used$, DISABLE) == 0)
    //
    // Set PA10 Pin Mode
    //	
    GPIO_InitStructure.GPIO_Pin = GPIO_Pin_10;
    GPIO_InitStructure.GPIO_Mode = $pin10Mode$;
    GPIO_InitStructure.GPIO_Speed = $pin10Speed$;
    GPIO_Init(GPIOB, &GPIO_InitStructure);
	#if(STRCMP($pin10Mode$, GPIO_Mode_Out_OD) != 0 || STRCMP($pin10Mode$, GPIO_Mode_Out_PP) != 0 || \
	STRCMP($pin10Mode$, GPIO_Mode_AF_OD) != 0 || STRCMP($pin10Mode$, GPIO_Mode_AF_PP) != 0)
    #if(STRCMP($pinData10$, RESET) == 0)
	//
    // Set or Reset PA10 bits of output data
    //
    GPIO_SetBits(GPIOB, GPIO_Pin_10);
    #endif
    #if(STRCMP($pinData10$, SET) == 0)
	//
    // Set or Reset PA10 bits of output data
    //
    GPIO_ResetBits(GPIOB, GPIO_Pin_10);
	#endif
    #endif
	#endif
	
    #if(STRCMP($pin11Used$, DISABLE) == 0)
    //
    // Set PA11 Pin Mode
    //	
    GPIO_InitStructure.GPIO_Pin = GPIO_Pin_11;
    GPIO_InitStructure.GPIO_Mode = $pin11Mode$;
    GPIO_InitStructure.GPIO_Speed = $pin11Speed$;
    GPIO_Init(GPIOB, &GPIO_InitStructure);
	#if(STRCMP($pin11Mode$, GPIO_Mode_Out_OD) != 0 || STRCMP($pin11Mode$, GPIO_Mode_Out_PP) != 0 || \
	STRCMP($pin11Mode$, GPIO_Mode_AF_OD) != 0 || STRCMP($pin11Mode$, GPIO_Mode_AF_PP) != 0)
    #if(STRCMP($pinData11$, RESET) == 0)
	//
    // Set or Reset PA11 bits of output data
    //
    GPIO_SetBits(GPIOB, GPIO_Pin_11);
    #endif
    #if(STRCMP($pinData11$, SET) == 0)
	//
    // Set or Reset PA11 bits of output data
    //
    GPIO_ResetBits(GPIOB, GPIO_Pin_11);
	#endif
    #endif
	#endif
	
    #if(STRCMP($pin12Used$, DISABLE) == 0)
    //
    // Set PA12 Pin Mode
    //	
    GPIO_InitStructure.GPIO_Pin = GPIO_Pin_12;
    GPIO_InitStructure.GPIO_Mode = $pin12Mode$;
    GPIO_InitStructure.GPIO_Speed = $pin12Speed$;
    GPIO_Init(GPIOB, &GPIO_InitStructure);
	#if(STRCMP($pin12Mode$, GPIO_Mode_Out_OD) != 0 || STRCMP($pin12Mode$, GPIO_Mode_Out_PP) != 0 || \
	STRCMP($pin12Mode$, GPIO_Mode_AF_OD) != 0 || STRCMP($pin12Mode$, GPIO_Mode_AF_PP) != 0)
    #if(STRCMP($pinData12$, RESET) == 0)
	//
    // Set or Reset PA12 bits of output data
    //
    GPIO_SetBits(GPIOB, GPIO_Pin_12);
    #endif
    #if(STRCMP($pinData12$, SET) == 0)
	//
    // Set or Reset PA12 bits of output data
    //
    GPIO_ResetBits(GPIOB, GPIO_Pin_12);
	#endif
    #endif
	#endif
	
    #if(STRCMP($pin13Used$, DISABLE) == 0)
    //
    // Set PA13 Pin Mode
    //	
    GPIO_InitStructure.GPIO_Pin = GPIO_Pin_13;
    GPIO_InitStructure.GPIO_Mode = $pin13Mode$;
    GPIO_InitStructure.GPIO_Speed = $pin13Speed$;
    GPIO_Init(GPIOB, &GPIO_InitStructure);
	#if(STRCMP($pin13Mode$, GPIO_Mode_Out_OD) != 0 || STRCMP($pin13Mode$, GPIO_Mode_Out_PP) != 0 || \
	STRCMP($pin13Mode$, GPIO_Mode_AF_OD) != 0 || STRCMP($pin13Mode$, GPIO_Mode_AF_PP) != 0)
    #if(STRCMP($pinData13$, RESET) == 0)
	//
    // Set or Reset PA13 bits of output data
    //
    GPIO_SetBits(GPIOB, GPIO_Pin_13);
    #endif
    #if(STRCMP($pinData13$, SET) == 0)
	//
    // Set or Reset PA13 bits of output data
    //
    GPIO_ResetBits(GPIOB, GPIO_Pin_13);
	#endif
    #endif
	#endif
	
    #if(STRCMP($pin14Used$, DISABLE) == 0)
    //
    // Set PA14 Pin Mode
    //	
    GPIO_InitStructure.GPIO_Pin = GPIO_Pin_14;
    GPIO_InitStructure.GPIO_Mode = $pin14Mode$;
    GPIO_InitStructure.GPIO_Speed = $pin14Speed$;
    GPIO_Init(GPIOB, &GPIO_InitStructure);
	#if(STRCMP($pin14Mode$, GPIO_Mode_Out_OD) != 0 || STRCMP($pin14Mode$, GPIO_Mode_Out_PP) != 0 || \
	STRCMP($pin14Mode$, GPIO_Mode_AF_OD) != 0 || STRCMP($pin14Mode$, GPIO_Mode_AF_PP) != 0)
    #if(STRCMP($pinData14$, RESET) == 0)
	//
    // Set or Reset PA14 bits of output data
    //
    GPIO_SetBits(GPIOB, GPIO_Pin_14);
    #endif
    #if(STRCMP($pinData14$, SET) == 0)
	//
    // Set or Reset PA14 bits of output data
    //
    GPIO_ResetBits(GPIOB, GPIO_Pin_14);
	#endif
    #endif
	#endif
	
    #if(STRCMP($pin15Used$, DISABLE) == 0)
    //
    // Set PA15 Pin Mode
    //	
    GPIO_InitStructure.GPIO_Pin = GPIO_Pin_15;
    GPIO_InitStructure.GPIO_Mode = $pin15Mode$;
    GPIO_InitStructure.GPIO_Speed = $pin15Speed$;
    GPIO_Init(GPIOB, &GPIO_InitStructure);
	#if(STRCMP($pin15Mode$, GPIO_Mode_Out_OD) != 0 || STRCMP($pin15Mode$, GPIO_Mode_Out_PP) != 0 || \
	STRCMP($pin15Mode$, GPIO_Mode_AF_OD) != 0 || STRCMP($pin15Mode$, GPIO_Mode_AF_PP) != 0)
    #if(STRCMP($pinData15$, RESET) == 0)
	//
    // Set or Reset PA15 bits of output data
    //
    GPIO_SetBits(GPIOB, GPIO_Pin_15);
    #endif
    #if(STRCMP($pinData15$, SET) == 0)
	//
    // Set or Reset PA15 bits of output data
    //
    GPIO_ResetBits(GPIOB, GPIO_Pin_15);
	#endif
    #endif
	#endif
	
    #if($pLockEn0$ | $pLockEn1$ | $pLockEn2$ | $pLockEn3$ | \
    $pLockEn4$ | $pLockEn5$ | $pLockEn6$ | $pLockEn7$ | \
    $pLockEn8$ | $pLockEn9$ | $pLockEn10$ | $pLockEn11$ | \
    $pLockEn12$ | $pLockEn13$ | $pLockEn14$ | $pLockEn15$)
    //PUT_A_NEW_LINE_HERE
    //
    // Lock configuration of GPIO pins.
    //
    GPIO_PinLockConfig($GPIO_PORT$, GPIO_LOCK_PIN);
	#endif	
}
